#include<iostream>
using namespace std;
int main()
{
    int a1, a2;
    cout<<"enter the value of a1 :\n"
    /* is called insertion operator*/ ;
    cin>>a1 /*is called extraction operator*/;

    cout<<"enter the value of a2 :\n"
    /* is called insertion operator*/ ;
    cin>>a2 /*is called extraction operator*/;

    cout<<"the sum is "<<a1+a2;
}